#include <stdio.h>
#include "sllist/llist.h"

void main() {

	ListNodePtr poly1, poly2;
}

void getPolynomial(ListNodePtr *head, int degree) {
	insertBeg(head);
	for (int i = 0; i < degree; i++) {
		printf("Enter value of x**%d: ");
		scanf("%d", &num);
		insertEnd
	}
		insert(&head, )
}
